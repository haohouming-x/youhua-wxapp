// API
// FRAME: 可按模块摆放接口url的枚举（注意不要带@符号，@符号为path参数解析的标注符号）
const API = {
  // 公共服务
  'globalUrl.getProvince': '/api/xxx', // 获取省市
  'globalUrl.getProvince.details': '/api/xxx/{id}', // 获取省市
}

export default API
